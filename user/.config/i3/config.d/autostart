#   █████  ██    ██ ████████  ██████  ███████ ████████  █████  ██████  ████████  #
#  ██   ██ ██    ██    ██    ██    ██ ██         ██    ██   ██ ██   ██    ██     #
#  ███████ ██    ██    ██    ██    ██ ███████    ██    ███████ ██████     ██     #
#  ██   ██ ██    ██    ██    ██    ██      ██    ██    ██   ██ ██   ██    ██     #
#  ██   ██  ██████     ██     ██████  ███████    ██    ██   ██ ██   ██    ██     #
                                                                             
### --- Установка различных настроек в систему. Экспорт переменных и т.д.
exec --no-startup-id "$i3_scripts/SetSysVars"
exec --no-startup-id "$i3_scripts/SetUser"
exec --no-startup-id "$i3_scripts/ThemeApply"
### ---------------------------------------------------------END------------------------------------------------------------- ###

### --- Стартуем один раз.

exec --no-startup-id nm-applet
exec --no-startup-id xfce4-power-manager
exec --no-startup-id udiskie -t
exec --no-startup-id thunar --daemon
### ---------------------------------------------------------END------------------------------------------------------------- ###

### --- Брелок для хранения сохраненных паролей.
exec --no-startup-id gnome-keyring-daemon --start --components=pkcs11
### ---------------------------------------------------------END------------------------------------------------------------- ###

### --- Polkit используется для управления общесистемными привилегиями. 
### --- Данный фреймворк используется для предоставления непривилегированным процессам возможность выполнения действий, требующих прав администратора.
### --- В отличие от sudo, Polkit не наделяет процесс правами суперпользователя, а позволяет точно контролировать, какие действия разрешены, а какие нет.
exec --no-startup-id /usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
### ---------------------------------------------------------END------------------------------------------------------------- ###

### --- Стартуем каждый раз при перезагрузки i3wm.

### --- Стартуем композитор, для прозрачности приложений и прочих эффектов рабочего окружения.
exec_always --no-startup-id picom -b --config $i3_configd/picom.conf
### ---------------------------------------------------------END------------------------------------------------------------- ###

### --- Стартуем Dunst уведомления.
#exec_always --no-startup-id $confd/dunst/dunst.sh
### ---------------------------------------------------------END------------------------------------------------------------- ###

### --- Загружаем цвета и всякие там красивости ИксРесурсов. Нужны обязательно. Отсюда цепляются настройки терминала Xterm или Urxvt
exec_always --no-startup-id xrdb -load $HOME/.Xresources
### ---------------------------------------------------------END------------------------------------------------------------- ###

### --- 
#exec_always --no-startup-id xsetroot -cursor_name left_ptr
### ---------------------------------------------------------END------------------------------------------------------------- ###

### --- Стартуем главную тему Catppuccin Mocha.
exec_always --no-startup-id $HOME/.config/i3/rices/catppuccin-mocha/Theme.sh
### ---------------------------------------------------------END------------------------------------------------------------- ###

### --- Стуртуем обоину. Если не указывать конкретную картинку (car_trip.jpg), 
### --- в таком случае картинка будет меняться рандомно с каждым стартом
exec_always --no-startup-id feh -z --no-fehbg --bg-fill $HOME/.config/i3/rices/catppuccin-mocha/walls/land.jpeg
### ---------------------------------------------------------END------------------------------------------------------------- ###

### --- Стартуем автотайлинг. Эта программа расставляет терминал по местам красиво.
exec_always --no-startup-id autotiling
### ---------------------------------------------------------END------------------------------------------------------------- ###

### --- Стартуем блокировщик экрана
exec_always --no-startup-id xss-lock --ignore-sleep ~/.config/i3/src/ScreenLocker
### ---------------------------------------------------------END------------------------------------------------------------- ###

### --- Стартуем эквалайзер Pipewire. Полезно для наушников. Отключить за ненадобностью.
exec --no-startup-id /usr/bin/nohup /usr/bin/easyeffects --gapplication-service
### ---------------------------------------------------------END------------------------------------------------------------- ###

### --- Стартуем инструмент для применение GTK тем и иконок "на лету"
exec --no-startup-id xsettingsd
### ---------------------------------------------------------END------------------------------------------------------------- ###