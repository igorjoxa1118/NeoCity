# Maintainer: vir0id

srcname=tealize-grub-theme
pkgname=virOS-grub-theme
pkgdesc='VirOS grub-theme'
pkgver=1.3.2
pkgrel=1
arch=('any')
url="https://github.com/igorjoxa1118/virOS-grub-theme"
license=('GPL')
makedepends=('git')
source=("git+$url.git")
sha256sums=('SKIP')

package() {
  cd ${srcdir}/${pkgname}/
  install -dm755 "${pkgdir}/usr/share/grub/themes"

  cp -rf tealize-grub-theme "${pkgdir}/usr/share/grub/themes"
}
