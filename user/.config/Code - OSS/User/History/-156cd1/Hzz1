# Maintainer: vir0id

srcname=virOS-mirrorlist
pkgname=virOS-mirrorlist
pkgdesc='VirOS mirrors'
pkgver=1.3.2
pkgrel=1
arch=('any')
url="https://github.com/igorjoxa1118/virOS-mirrorlist"
license=('GPL')
makedepends=('git')
source=("git+$url.git")
sha256sums=('SKIP')

package() {
  cd ${srcdir}/${pkgname}/
  install -dm755 "${pkgdir}/etc/pacman.d/"
  cp -rf virOS-mirrorlist "${pkgdir}/etc/pacman.d/"
}
