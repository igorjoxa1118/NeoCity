# Maintainer: vir0id

srcname=virOS-fonts
pkgname=virOS-fonts
pkgdesc='Fonts for virOS'
pkgver=1.0
pkgrel=1
arch=('any')
url="https://github.com/igorjoxa1118/virOS-fonts"
license=('GPL')
makedepends=('git')
depends=('i3-wm')
source=("git+$url.git")
sha256sums=('SKIP')

package() {
  cd ${srcdir}/${srcname}/
  install -dm755 "${pkgdir}/usr/share/fonts"

  cp -rf .fonts/* "$pkgdir/usr/share/fonts"
}